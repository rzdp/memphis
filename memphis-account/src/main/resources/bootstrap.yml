spring:
  application:
    name: memphis-account
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev
server:
  port: 8001
logging:
  level:
    com.rzdp.memphis.account: DEBUG
encrypt:
  key: ${ENCRYPT_KEY}