spring:
  application:
    name: memphis-organization
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev
server:
  port: 8000
logging:
  level:
    com.rzdp.memphis.organization: DEBUG
